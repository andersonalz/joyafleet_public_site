import OperationsDispatch from '../../../views/OperationsDispatch';
import { createPageMetadata } from '../../metadata';
export const metadata = createPageMetadata({ title: 'Flight Operations & Dispatch Software | JoyaFleet Flight Management Platform', description: 'JoyaFleet Flight Operations & Dispatch Software connects operational trips, dispatch workflows, flight records, journey history and reporting within a cloud-based Flight Management Software platform.', path: '/platform/operations-dispatch', });
export default function Page() { return <OperationsDispatch />; }
