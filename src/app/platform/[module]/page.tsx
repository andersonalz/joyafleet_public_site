import PlatformDetailPlaceholder from '../../../views/PlatformDetailPlaceholder';
import { createPageMetadata } from '../../metadata';
export const metadata = createPageMetadata({ title: 'Module Not Found | Joya Fleet', description: 'The requested Joya Fleet platform module is not available.', path: '/platform', });
export default function Page() { return <PlatformDetailPlaceholder />; }
