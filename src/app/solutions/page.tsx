import Solutions from '../../views/Solutions';
import { createPageMetadata } from '../metadata';
export const metadata = createPageMetadata({ title: 'Solutions for Airline Operating Models | Joya Fleet', description: 'Explore how Joya Fleet supports business aviation, charter, scheduled and cargo operators through connected, configurable flight-operations workflows.', path: '/solutions', });
export default function Page() { return <Solutions />; }
